const TaskEditISpage = ({ params }: { params: { id: string } }) => {
  return (
    <div>
      TaskEditISpage
      <h1>TaskEditISpage {params.id}</h1>
    </div>
  );
};

export default TaskEditISpage;
